<!DOCTYPE html>
<html lang="en" xmlns="http://www.w3.org/1999/xhtml">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>




<table class="container">
    <tbody>
    <tr th:each="wish :${wishList}">
        <td th:text="${wish.name}"></td>
        <td th:text="${wish.price}"></td>
        <td th:text="${wish.reserved}"></td>
    </tr>
    </tbody>
</table>

<button id="AddButton">Add Wish</button>
<button id="DeleteButton">Delete Wish</button>
<button id="LogoutButton">Logout</button>


</body>
</html>



















<!DOCTYPE html>
<html lang="en" xmlns="http://www.w3.org/1999/xhtml">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta charset="UTF-8">
    <title>Title</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheetWishlist"
          integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="stylesheetWishlist.css">
</head>
<body>
<center>
    <div class="container">
        <div class="row">
            <form action="/wishlist" method="post">
                <div class="col-sm">
                    Oeach="wish :${wishList}
                </div>

                    <tr th:each="wish :${wishList}">
                        <td th:text="${wish.name}"></td>
                        <td th:text="${wish.price}"></td>
                        <td th:text="${wish.reserved}"></td>
                    </tr>

                    <button>Add Wish</button>
                    <button>Delete Wish</button>
                    <button id="LogoutButton">Logout</button>
            </form>
                </div>


    </div>

</center>
</body>
</html>







#wishboks {
    background-color: lightgray !important;
    width: 38% !important;
    margin-top: 13% !important;
    font-size: 230% !important;
    text-align: center !important;
    border: 2px solid black !important;
    padding-bottom: 1% !important;
    padding-left: 1%;
    padding-right: 1%;
}